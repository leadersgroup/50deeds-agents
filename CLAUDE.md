# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is **empty** — no source files, no commits on `main`, and the remote
(`https://github.com/leadersgroup/50deeds-agents.git`) has no refs.

There are therefore no build, test, or lint commands to document yet, and no architecture to
describe. Everything below is the only verified information about this repo.

- Remote: `origin` → `https://github.com/leadersgroup/50deeds-agents.git`
- Default branch: `main` (unborn — the first commit will create it)
- The first push needs `git push -u origin main` to set upstream.

## Before working here

Re-run `/init` once the project has real code. This file should be replaced with actual build/test
commands and an architecture overview at that point — do not treat the absence of those sections as
a sign the project has no conventions, only that none had been committed when this file was written.
